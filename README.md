# Overview

This is a pet project. 
The idea is to process a folder that contain videos with different llms to detect different attributes,
store them in a DB, and to be able to query the video DB against the attributes.

E.g. video that has a particular topic, phrase, objects.

# Other thoughts
In the ideal world, it would be awesome to figure out a way to recognize bboying moves,
such that it is possible to assign and query by move names.
